# Shioaji 交易系統類別圖

本文件使用 Mermaid 語法描述 Shioaji 交易客戶端的類別結構。

## 類別圖

```mermaid
classDiagram
    class IConfigValidator {
        <<interface>>
        +validate() void
    }

    class ITradingClient {
        <<interface>>
        +connect(config: Any) bool
        +disconnect() bool
        +get_accounts() Dict~str, Any~
        +is_connected() bool
        +get_contracts() Any
    }
    
    class IQuoteCallback {
        <<interface>>
        +on_quote(topic: str, quote: Any) void
    }
    
    class IOrderCallback {
        <<interface>>
        +on_order(stat: str, order: Any) void
        +on_deal(stat: str, deal: Any) void
    }

    class LoginConfig {
        +str person_id
        +str passwd
        +Optional~str~ ca_path
        +Optional~str~ ca_passwd
        +bool simulation
        +validate() void
    }

    class ShioajiClient {
        -Optional~Shioaji~ sj
        -bool is_logged_in
        -Optional~LoginConfig~ config
        -Optional~Any~ contracts
        -Optional~IQuoteCallback~ quote_callback
        -Optional~IOrderCallback~ order_callback
        -List~str~ subscribed_quotes
        -Logger logger
        +__init__()
        +connect(config: LoginConfig) bool
        +disconnect() bool
        +login(config: LoginConfig) bool
        +logout() bool
        +get_accounts() Dict~str, Any~
        +is_connected() bool
        +get_contracts() Any
        +search_contracts(keyword: str) list
        +get_stock(code: str) Any
        +set_quote_callback(callback: IQuoteCallback) void
        +set_order_callback(callback: IOrderCallback) void
        +register_quote_callback() bool
        +register_order_callback() bool
        +subscribe_quote(contract: Any) bool
        +unsubscribe_quote(contract: Any) bool
        +get_subscribed_quotes() List~str~
        +__enter__() ShioajiClient
        +__exit__(exc_type, exc_val, exc_tb)
    }
    
    class DefaultQuoteCallback {
        -Logger logger
        -Optional~Callable~ custom_handler
        -int quote_count
        +__init__(custom_handler)
        +on_quote(topic: str, quote: Any) void
    }
    
    class DefaultOrderCallback {
        -Logger logger
        -Optional~Callable~ order_handler
        -Optional~Callable~ deal_handler
        -int order_count
        -int deal_count
        +__init__(order_handler, deal_handler)
        +on_order(stat: str, order: Any) void
        +on_deal(stat: str, deal: Any) void
    }

    class Shioaji {
        <<external>>
        +login(person_id, passwd)
        +activate_ca(ca_path, ca_passwd, person_id)
        +logout()
        +stock_account
        +futopt_account
        +Contracts
    }
    
    class Contracts {
        <<external>>
        +Stocks
        +Futures
        +Options
        +Indexs
        +search(keyword)
    }

    LoginConfig ..|> IConfigValidator : implements
    ShioajiClient ..|> ITradingClient : implements
    ShioajiClient --> LoginConfig : uses
    ShioajiClient --> Shioaji : contains
    ShioajiClient --> Contracts : accesses
    ShioajiClient --> IQuoteCallback : uses
    ShioajiClient --> IOrderCallback : uses
    ShioajiClient ..|> ContextManager : implements
    Shioaji --> Contracts : contains
    DefaultQuoteCallback ..|> IQuoteCallback : implements
    DefaultOrderCallback ..|> IOrderCallback : implements

    note for IConfigValidator "配置驗證器介面\n定義配置驗證的標準方法"
    note for ITradingClient "交易客戶端抽象介面\n遵循依賴反轉原則 (DIP)\n包含商品檔取得方法"
    note for IQuoteCallback "報價回調介面\n處理即時報價資料"
    note for IOrderCallback "訂單回調介面\n處理訂單狀態和成交通知"
    note for LoginConfig "資料類別 (Dataclass)\n封裝登入所需的所有配置參數\n實作配置驗證邏輯"
    note for ShioajiClient "主要客戶端類別\n實作 ITradingClient 介面\n負責管理 API 生命週期、商品檔與報價訂閱"
    note for DefaultQuoteCallback "預設報價回調實作\n提供基本的報價處理和日誌記錄"
    note for DefaultOrderCallback "預設訂單回調實作\n提供基本的訂單和成交處理"
    note for Shioaji "第三方 Shioaji SDK\n由永豐金證券提供"
    note for Contracts "商品檔物件\n包含股票、期貨、選擇權、指數等商品資訊"
```

## 類別說明

### IConfigValidator (介面)
- **用途**: 定義配置驗證的標準介面
- **設計模式**: Interface Segregation Principle (ISP)
- **方法**:
  - `validate()`: 驗證配置的有效性

### ITradingClient (介面)
- **用途**: 定義交易客戶端的標準介面
- **設計模式**: Dependency Inversion Principle (DIP)
- **方法**:
  - `connect(config)`: 連接到交易系統
  - `disconnect()`: 斷開連線
  - `get_accounts()`: 取得帳戶資訊
  - `is_connected()`: 檢查連線狀態
  - `get_contracts()`: 取得商品檔資訊
- **優點**:
  - 高層模組依賴於抽象介面，而非具體實作
  - 便於測試（可使用 Mock 物件）
  - 支援多型性，可替換不同的實作

### LoginConfig
- **用途**: 封裝登入配置資訊
- **設計模式**: Data Transfer Object (DTO)
- **實作介面**: IConfigValidator
- **屬性**:
  - `person_id`: 使用者身分證字號或統一編號
  - `passwd`: 使用者密碼
  - `ca_path`: CA 憑證檔案路徑（可選）
  - `ca_passwd`: CA 憑證密碼（可選）
  - `simulation`: 是否使用模擬環境
- **方法**:
  - `validate()`: 驗證配置參數的有效性

### IQuoteCallback (介面)
- **用途**: 定義報價回調處理的標準介面
- **設計模式**: Strategy Pattern (策略模式)
- **方法**:
  - `on_quote(topic, quote)`: 處理報價回調
- **優點**:
  - 允許自訂報價處理邏輯
  - 支援多種實作方式
  - 遵循開放封閉原則

### IOrderCallback (介面)
- **用途**: 定義訂單回調處理的標準介面
- **設計模式**: Strategy Pattern (策略模式)
- **方法**:
  - `on_order(stat, order)`: 處理訂單狀態回調
  - `on_deal(stat, deal)`: 處理成交回調
- **優點**:
  - 分離訂單和成交處理邏輯
  - 便於擴展和自訂

### ShioajiClient
- **用途**: Shioaji API 的封裝類別
- **實作介面**: ITradingClient
- **設計模式**: 
  - Facade Pattern (外觀模式)
  - Context Manager Pattern
  - Adapter Pattern (適配器模式)
  - Observer Pattern (觀察者模式 - callback 機制)
- **主要職責**:
  - 管理 Shioaji API 實例的生命週期
  - 提供統一的登入/登出介面
  - 管理商品檔資訊
  - 管理報價訂閱和 callback
  - 處理錯誤和日誌記錄
  - 將 Shioaji SDK 適配到 ITradingClient 介面
- **屬性**:
  - `sj`: Shioaji API 實例（登入後可用）
  - `is_logged_in`: 登入狀態標記
  - `config`: 登入配置資訊
  - `contracts`: 商品檔物件（登入後自動載入）
  - `quote_callback`: 報價回調處理器
  - `order_callback`: 訂單回調處理器
  - `subscribed_quotes`: 已訂閱的商品列表
  - `logger`: 日誌記錄器
- **方法**:
  - `connect()`: 連接到交易系統（實作 ITradingClient）
  - `disconnect()`: 斷開連線（實作 ITradingClient）
  - `login()`: 執行登入操作
  - `logout()`: 執行登出操作
  - `get_accounts()`: 取得帳戶資訊（實作 ITradingClient）
  - `is_connected()`: 檢查連線狀態（實作 ITradingClient）
  - `get_contracts()`: 取得商品檔資訊（實作 ITradingClient）
  - `search_contracts()`: 搜尋商品檔
  - `get_stock()`: 取得特定股票商品
  - `set_quote_callback()`: 設定報價回調處理器
  - `set_order_callback()`: 設定訂單回調處理器
  - `register_quote_callback()`: 註冊報價回調
  - `register_order_callback()`: 註冊訂單回調
  - `subscribe_quote()`: 訂閱報價
  - `unsubscribe_quote()`: 取消訂閱報價
  - `get_subscribed_quotes()`: 取得已訂閱商品列表
  - `__enter__()`, `__exit__()`: 支援 with 語句的 context manager

### DefaultQuoteCallback
- **用途**: 預設報價回調實作
- **實作介面**: IQuoteCallback
- **主要功能**:
  - 將報價資訊記錄到日誌
  - 支援自訂處理函數
  - 統計報價接收數量
- **屬性**:
  - `logger`: 日誌記錄器
  - `custom_handler`: 自訂處理函數（可選）
  - `quote_count`: 報價接收計數
- **方法**:
  - `on_quote()`: 處理報價回調

### DefaultOrderCallback
- **用途**: 預設訂單回調實作
- **實作介面**: IOrderCallback
- **主要功能**:
  - 將訂單和成交資訊記錄到日誌
  - 支援自訂處理函數
  - 分別統計訂單和成交數量
- **屬性**:
  - `logger`: 日誌記錄器
  - `order_handler`: 訂單自訂處理函數（可選）
  - `deal_handler`: 成交自訂處理函數（可選）
  - `order_count`: 訂單接收計數
  - `deal_count`: 成交接收計數
- **方法**:
  - `on_order()`: 處理訂單狀態回調
  - `on_deal()`: 處理成交回調

### Shioaji
- **用途**: 第三方 SDK
- **來源**: 永豐金證券
- **主要功能**: 提供台灣金融市場交易 API
- **主要屬性**:
  - `stock_account`: 股票帳戶
  - `futopt_account`: 期貨選擇權帳戶
  - `Contracts`: 商品檔物件

### Contracts
- **用途**: 商品檔物件
- **來源**: Shioaji SDK
- **主要功能**: 儲存所有可交易的商品資訊
- **主要屬性**:
  - `Stocks`: 股票商品（台股、興櫃等）
  - `Futures`: 期貨商品（台指期、小台等）
  - `Options`: 選擇權商品
  - `Indexs`: 指數商品（加權指數、櫃買指數等）
- **方法**:
  - `search(keyword)`: 搜尋商品
- **更新時間**:
  - 07:50 期貨商品檔更新
  - 08:00 全市場商品檔更新
  - 14:45 期貨夜盤商品檔更新
  - 17:15 期貨夜盤商品檔更新

## 設計特點 - SOLID 原則實踐

### 1. 單一職責原則 (Single Responsibility Principle) ✅
- **IConfigValidator**: 只定義配置驗證介面
- **ITradingClient**: 只定義交易客戶端介面
- **LoginConfig**: 只負責封裝和驗證配置資料
- **ShioajiClient**: 只負責管理 Shioaji API 的生命週期和基本操作
- **職責分離明確**: 配置驗證、交易操作、日誌記錄各司其職

### 2. 開放封閉原則 (Open/Closed Principle) ✅
- 通過抽象介面 `ITradingClient` 和 `IConfigValidator` 實現對擴展開放
- 可以創建新的交易客戶端實作而不需修改現有程式碼
- 核心邏輯對修改封閉，對擴展開放
- 範例：可以實作 `MockTradingClient` 用於測試，無需修改原有程式碼

### 3. 里氏替換原則 (Liskov Substitution Principle) ✅
- 所有實作 `ITradingClient` 的類別都可以互相替換
- 實作標準的 Context Manager 協議
- 可以在任何需要 `ITradingClient` 的地方使用 `ShioajiClient`
- 子類別不會破壞父類別的行為契約

### 4. 介面隔離原則 (Interface Segregation Principle) ✅
- `ITradingClient` 只包含交易客戶端必需的方法
- `IConfigValidator` 只包含配置驗證必需的方法
- 介面精簡、專注，客戶端不需依賴不使用的方法
- 避免"胖介面"問題

### 5. 依賴反轉原則 (Dependency Inversion Principle) ✅
- **高層模組依賴抽象**: 使用 `ITradingClient` 介面而非具體實作
- **抽象不依賴細節**: `ITradingClient` 不依賴 Shioaji SDK 的具體實作
- **細節依賴抽象**: `ShioajiClient` 實作 `ITradingClient` 介面
- **便於測試**: 可以使用 Mock 物件替換真實的 Shioaji SDK
- **降低耦合**: 通過 `LoginConfig` 資料類別解耦配置和實作

## SOLID 原則帶來的好處

1. **可測試性 (Testability)**
   - 透過抽象介面，可以輕鬆使用 Mock 物件進行單元測試
   - 不需要真實的 Shioaji API 連線即可測試業務邏輯

2. **可維護性 (Maintainability)**
   - 職責分離明確，修改某一部分不影響其他部分
   - 程式碼結構清晰，易於理解和修改

3. **可擴展性 (Extensibility)**
   - 可以輕鬆添加新的交易客戶端實作（如其他券商的 API）
   - 可以通過繼承或組合擴展功能

4. **可重用性 (Reusability)**
   - 抽象介面可以在不同專案中重用
   - 配置類別和驗證邏輯可以獨立使用

5. **低耦合高內聚 (Low Coupling, High Cohesion)**
   - 模組間依賴關係清晰
   - 每個類別內部高度內聚

## 使用流程圖

### 登入與取得商品檔流程

```mermaid
sequenceDiagram
    participant User
    participant ShioajiClient
    participant LoginConfig
    participant Shioaji
    participant Contracts

    User->>LoginConfig: 建立配置
    User->>ShioajiClient: 建立客戶端
    User->>ShioajiClient: login(config)
    ShioajiClient->>Shioaji: 建立實例
    ShioajiClient->>Shioaji: login(person_id, passwd)
    Shioaji-->>ShioajiClient: 返回帳戶列表
    alt 如果有 CA 憑證
        ShioajiClient->>Shioaji: activate_ca(...)
    end
    ShioajiClient->>Shioaji: 取得 Contracts
    Shioaji-->>ShioajiClient: 商品檔物件
    ShioajiClient-->>ShioajiClient: 儲存至 contracts 屬性
    ShioajiClient-->>User: 返回成功狀態
    
    User->>ShioajiClient: get_accounts()
    ShioajiClient->>Shioaji: 取得帳戶資訊
    Shioaji-->>ShioajiClient: 帳戶資料
    ShioajiClient-->>User: 返回帳戶資訊
    
    User->>ShioajiClient: get_contracts()
    ShioajiClient-->>User: 返回商品檔物件
    
    User->>ShioajiClient: logout()
    ShioajiClient->>Shioaji: logout()
    Shioaji-->>ShioajiClient: 成功
    ShioajiClient-->>User: 返回登出狀態
```

### 商品檔查詢流程

```mermaid
sequenceDiagram
    participant User
    participant ShioajiClient
    participant Contracts

    User->>ShioajiClient: search_contracts("2330")
    ShioajiClient->>Contracts: search("2330")
    Contracts-->>ShioajiClient: 搜尋結果列表
    ShioajiClient-->>User: 返回搜尋結果
    
    User->>ShioajiClient: get_stock("2330")
    ShioajiClient->>Contracts: Stocks["2330"]
    Contracts-->>ShioajiClient: 股票物件
    ShioajiClient-->>User: 返回股票資訊
    
    User->>ShioajiClient: get_contracts()
    ShioajiClient-->>User: 商品檔物件
    User->>Contracts: Futures.TXF
    Contracts-->>User: 期貨合約列表
    User->>Contracts: Indexs.TSE.TSE001
    Contracts-->>User: 加權指數物件
```

## 錯誤處理流程

```mermaid
flowchart TD
    A[開始登入] --> B{驗證參數}
    B -->|參數無效| C[拋出 ValueError]
    B -->|參數有效| D[建立 Shioaji 實例]
    D --> E[執行登入]
    E -->|連線失敗| F[拋出 ConnectionError]
    E -->|登入成功| G{是否有 CA 憑證?}
    G -->|是| H[啟用 CA 憑證]
    G -->|否| I[設定登入狀態]
    H --> I
    I --> J[返回成功]
    E -->|其他錯誤| K[拋出 RuntimeError]
    
    C --> L[記錄錯誤日誌]
    F --> L
    K --> L
    L --> M[結束]
    J --> M
```

## 擴展建議

未來可以考慮以下擴展：

1. **策略模式**: 支援不同的登入策略（基本登入、CA 登入、OAuth 等）
2. **觀察者模式**: 實作事件監聽機制，監控帳戶狀態變化
3. **工廠模式**: 建立不同類型的交易客戶端（股票、期貨、選擇權）
4. **裝飾器模式**: 添加交易記錄、風險控制等額外功能
5. **命令模式**: 封裝交易指令，支援撤銷、重做等操作
